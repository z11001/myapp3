<h1>Listing addresses</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @addresses.each do |address| %>
  <tr>
    <td><%= address.name %></td>
    <td><%= address.email %></td>
    <td><%= link_to 'Show', address %></td>
    <td><%= link_to 'Edit', edit_address_path(address) %></td>
    <td><%= link_to 'Destroy', address, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

#<%= link_to 'New Address', new_address_path %>
<%= link_to 'first', :controller => 'spots', :action => 'index' %>
<%= link_to 'SpotRegist', :controller => 'spot_regist', :action => 'index' %>
<%= link_to 'SpotSearch', :controller => 'spot_search', :action => 'index' %>